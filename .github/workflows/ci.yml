name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  node:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm --filter @devpilot/desktop build:web

  mobile:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true
      - working-directory: apps/mobile
        run: flutter pub get
      - working-directory: apps/mobile
        run: flutter analyze
      - working-directory: apps/mobile
        run: flutter test
      # Keep the deterministic change-flow fixture buildable and analyze-clean.
      - working-directory: fixtures/sample_flutter_app
        run: flutter pub get
      - working-directory: fixtures/sample_flutter_app
        run: flutter analyze
      - working-directory: fixtures/sample_flutter_app
        run: flutter test

  change-flow-smoke:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true
      - run: pnpm install --frozen-lockfile
      - working-directory: fixtures/sample_flutter_app
        run: flutter pub get
      # Runs the Point & Fix generate -> validate -> apply -> revert flow with a
      # deterministic fake Codex provider and the real Flutter validation gate.
      - run: pnpm --filter @devpilot/agent test
        env:
          DEVPILOT_SMOKE_WITH_FLUTTER: '1'

  desktop-rust:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.98.0
      - run: pnpm install --frozen-lockfile
      - working-directory: apps/desktop/src-tauri
        run: cargo check --locked
